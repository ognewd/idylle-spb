# 🔧 Исправление: "Всего товаров: 0" в админ-панели

## ✅ Статус

- ✅ В локальной БД есть **1045 товаров**
- ✅ Публичный API возвращает товары
- ❌ Админ-панель показывает "Всего товаров: 0"

## 🔍 Проблема

Админ-панель требует авторизации через Bearer token. Возможные причины:

1. **Токен не валиден** - токен истек или неправильный
2. **Пользователь не найден** - администратор не существует в локальной БД
3. **Неправильные права** - пользователь не имеет роли admin/super_admin

## ✅ Решение

### Вариант 1: Создать администратора в локальной БД

```bash
# Запустить seed скрипт
npm run db:seed
```

Это создаст администратора:
- Email: `admin@idylle.spb.ru`
- Пароль: `admin123`

### Вариант 2: Проверить существующего администратора

1. Откройте консоль браузера (F12)
2. Проверьте, есть ли ошибки авторизации
3. Попробуйте выйти и войти заново

### Вариант 3: Проверить через Prisma Studio

```bash
npm run db:studio
```

Откройте таблицу `users` и проверьте:
- Есть ли пользователи с ролью `admin` или `super_admin`
- Если нет - создайте администратора

## 🔍 Диагностика

Проверьте в консоли браузера (F12):
- Ошибки в Network tab при загрузке `/api/admin/products`
- Статус ответа (должен быть 200, не 401)
- Проверьте, передается ли токен в заголовках

## 📝 Быстрое решение

1. Откройте `/admin/login`
2. Войдите как администратор
3. Если нет аккаунта - запустите `npm run db:seed`
4. Попробуйте снова зайти в `/admin/products`

