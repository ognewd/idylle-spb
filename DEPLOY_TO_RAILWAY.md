# 🚂 Деплой на Railway - Быстрая инструкция

## 📋 Пошаговая инструкция

### 1. Создать аккаунт и проект на Railway

1. Откройте https://railway.app
2. Нажмите "Start a New Project" или "Login with GitHub"
3. Войдите через GitHub (рекомендуется)

### 2. Создать проект

1. Нажмите "+ New Project"
2. Выберите "Deploy from GitHub repo"
3. Найдите и выберите репозиторий `idylle-spb`
4. Railway автоматически начнет деплой

### 3. Добавить PostgreSQL базу данных

1. В проекте Railway нажмите "+ New"
2. Выберите "Database" → "Add PostgreSQL"
3. Railway автоматически создаст базу и добавит переменную `DATABASE_URL`

### 4. Настроить переменные окружения

1. В проекте Railway нажмите на ваше приложение (не на базу данных)
2. Перейдите в раздел "Variables" (Переменные окружения)
3. Добавьте переменные:

```env
NEXTAUTH_SECRET=uvZuO6y91hb2zbxwY3EmtqKH9YeJSyT58AH5nMR3KWY=
NEXTAUTH_URL=https://your-app-name.up.railway.app
NEXT_PUBLIC_BASE_URL=https://your-app-name.up.railway.app

NEXT_PUBLIC_APP_NAME=Idylle
NEXT_PUBLIC_APP_DESCRIPTION=Люксовые парфюмы и товары для дома
```

**Примечание:** 
- `DATABASE_URL` Railway добавит автоматически
- `NEXTAUTH_URL` и `NEXT_PUBLIC_BASE_URL` - обновите после получения URL

### 5. Получить URL приложения

1. В Railway Dashboard откройте ваш проект
2. Нажмите на приложение (не на базу данных)
3. Найдите раздел "Settings" → "Domains"
4. Там будет URL вашего приложения (например: `https://idylle-spb-production-xxxx.up.railway.app`)
5. Скопируйте URL

### 6. Обновить NEXTAUTH_URL и NEXT_PUBLIC_BASE_URL

1. Вернитесь в "Variables"
2. Обновите:
   - `NEXTAUTH_URL` = ваш URL из шага 5
   - `NEXT_PUBLIC_BASE_URL` = ваш URL из шага 5
3. Railway автоматически пересоберет приложение

### 7. Применить миграции базы данных

**Вариант A: Через Railway CLI**

```bash
# Установить Railway CLI
npm i -g @railway/cli

# Войти в Railway
railway login

# Подключиться к проекту
cd /Users/dognev/idylle-spb
railway link

# Применить схему базы данных
railway run npx prisma db push

# (Опционально) Заполнить тестовыми данными
railway run npm run db:seed
```

**Вариант B: Через Railway Dashboard**

1. В проекте Railway откройте ваше приложение
2. Перейдите в раздел "Deployments"
3. Найдите последний деплой
4. Откройте "Shell" (терминал)
5. Выполните команды:
   ```bash
   npx prisma db push
   npm run db:seed  # опционально
   ```

### 8. Проверить работу

1. Откройте URL вашего приложения из Railway
2. Проверьте главную страницу
3. Попробуйте войти в админ-панель

## ✅ Преимущества Railway

- ✅ Все в одном месте (приложение + база данных)
- ✅ Бесплатный PostgreSQL
- ✅ Автоматический HTTPS
- ✅ Простой деплой
- ✅ Автоматические деплои при пуше в Git

## 🔗 Полезные ссылки

- Railway Dashboard: https://railway.app/dashboard
- Railway CLI: https://docs.railway.app/develop/cli
- Railway Docs: https://docs.railway.app

