# 🔧 Решение ошибки "Resource provisioning failed"

## 🔍 Проблема

На скриншоте Vercel видно:
- **Статус**: "Build Failed"
- **Ошибка**: "Resource provisioning failed"
- **Время ошибки**: 1 секунда (очень быстро - до начала сборки)
- **Раздел**: "Provisioning Integrations" показывает ошибку

Это означает, что **Vercel не может подготовить интеграцию** (скорее всего Supabase) перед началом сборки.

## ✅ Решение

### Шаг 1: Проверьте интеграцию Supabase в Vercel

1. Перейдите на https://vercel.com/dognevs-projects/idylle-spb/settings/integrations
2. Найдите интеграцию **Supabase** (если она есть)
3. Проверьте статус интеграции

### Шаг 2: Переподключите интеграцию (если она есть)

1. Отключите интеграцию Supabase
2. Подождите несколько минут
3. Подключите интеграцию заново
4. Проверьте, что подключение успешно

### Шаг 3: Удалите интеграцию (если не нужна)

Если проект использует только `DATABASE_URL` напрямую (без интеграции Vercel):

1. Перейдите на https://vercel.com/dognevs-projects/idylle-spb/settings/integrations
2. Удалите интеграцию Supabase (если она есть)
3. Убедитесь, что `DATABASE_URL` настроен вручную в Environment Variables

### Шаг 4: Проверьте переменную DATABASE_URL

1. Перейдите на https://vercel.com/dognevs-projects/idylle-spb/settings/environment-variables
2. Проверьте, что `DATABASE_URL` для Production содержит:
   ```
   postgresql://postgres:[YOUR_PASSWORD]@db.[YOUR_PROJECT].supabase.co:5432/postgres
   ```
3. Убедитесь, что пароль и проект ID правильные

### Шаг 5: Проверьте лимиты Supabase

1. Перейдите на https://supabase.com/dashboard
2. Откройте проект `idylle-spb`
3. Проверьте:
   - Не исчерпаны ли лимиты проекта
   - Активен ли проект
   - Не приостановлен ли проект

### Шаг 6: Попробуйте новый деплой

После исправления:

```bash
cd /Users/dognev/idylle-spb
vercel --prod
```

## 🎯 Рекомендуемое решение

**Самый простой способ:**

1. **Удалите интеграцию Supabase** из Vercel (если она есть)
2. **Используйте только `DATABASE_URL`** напрямую
3. **Проверьте**, что `DATABASE_URL` содержит полную строку подключения из Supabase
4. **Запустите новый деплой**

Причина: Ваш код использует только `DATABASE_URL` из переменных окружения (см. `src/lib/prisma.ts`), поэтому интеграция Vercel может быть лишней и вызывать конфликты.

## 📝 После исправления

После исправления проверьте:
- ✅ Новый деплой не показывает "Resource provisioning failed"
- ✅ Сборка начинается (Build Logs показывают прогресс)
- ✅ Сайт работает на https://idylle-spb.vercel.app

## ⚠️ Важно

**Старый деплой работает нормально**, что означает:
- Код исправен ✅
- База данных доступна ✅
- Переменные окружения настроены ✅

Проблема только в процессе деплоя новых версий.

