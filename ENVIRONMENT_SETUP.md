# 🚀 Настройка сред разработки

## 📋 Обзор

Проект настроен с разделением баз данных для dev и prod сред:

- **DEV**: Локальная PostgreSQL база данных
- **PROD**: Supabase PostgreSQL база данных

## 🔧 DEV Environment (Локальная разработка)

### Требования:
- PostgreSQL 15 (установлен через Homebrew)
- Node.js 18+

### Настройка:
```bash
# 1. Запустить PostgreSQL
brew services start postgresql@15

# 2. Создать базу данных (уже создана)
createdb idylle_spb_dev

# 3. Применить схему
export DATABASE_URL="postgresql://dognev@localhost:5432/idylle_spb_dev"
npx prisma db push

# 4. Заполнить тестовыми данными
npx ts-node scripts/seed-dev-database.ts

# 5. Запустить приложение
export DATABASE_URL="postgresql://dognev@localhost:5432/idylle_spb_dev"
npm run dev
```

### Доступ:
- **URL**: http://localhost:3000
- **База данных**: `idylle_spb_dev` (локальная)
- **Админка**: admin@idylle.spb.ru / admin123

## 🌐 PROD Environment (Vercel)

### Настройка:
```bash
# Деплой на Vercel
vercel --prod
```

### Доступ:
- **URL**: https://idylle-spb.vercel.app
- **База данных**: Supabase PostgreSQL
- **Админка**: admin@idylle.spb.ru / admin123

## 🔄 Переключение между средами

### Для разработки (DEV):
```bash
export DATABASE_URL="postgresql://dognev@localhost:5432/idylle_spb_dev"
npm run dev
```

### Для продакшена (PROD):
```bash
vercel --prod
```

## 📊 Преимущества разделения

✅ **Безопасность**: Изменения в dev не влияют на prod
✅ **Изоляция данных**: Отдельные базы данных
✅ **Тестирование**: Можно экспериментировать без риска
✅ **Производительность**: Локальная БД быстрее для разработки

## 🛠️ Полезные команды

```bash
# Проверить подключение к БД
npx prisma db pull

# Сбросить локальную БД
dropdb idylle_spb_dev && createdb idylle_spb_dev

# Пересоздать данные
npx ts-node scripts/seed-dev-database.ts

# Посмотреть данные в БД
psql idylle_spb_dev
```

## 🚨 Важно

- **DEV база**: Используется только для разработки
- **PROD база**: Используется только для продакшена
- **Не смешивайте данные** между средами!

