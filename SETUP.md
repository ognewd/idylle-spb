# 🚀 Инструкции по запуску проекта Idylle

## Предварительные требования

- Node.js 18+ 
- PostgreSQL база данных
- npm или yarn

## 1. Установка зависимостей

```bash
npm install
```

## 2. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта со следующим содержимым:

```env
# Database
DATABASE_URL="postgresql://username:password@localhost:5432/idylle_spb"

# NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-here"

# Payment Providers (опционально для разработки)
STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_SECRET_KEY="sk_test_..."
YOOKASSA_SHOP_ID="your-shop-id"
YOOKASSA_SECRET_KEY="your-secret-key"
TINKOFF_TERMINAL_KEY="your-terminal-key"
TINKOFF_SECRET_KEY="your-secret-key"

# Email Configuration (опционально для разработки)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"

# Company Details for Bank Transfer
COMPANY_NAME="ООО Идилль"
COMPANY_INN="1234567890"
COMPANY_KPP="123456789"
COMPANY_BANK_NAME="ПАО Сбербанк"
COMPANY_BANK_ACCOUNT="40702810123456789012"
COMPANY_BANK_BIK="044525225"
COMPANY_BANK_CORR_ACCOUNT="30101810400000000225"
COMPANY_ADDRESS="г. Санкт-Петербург, ул. Невский проспект, д. 1"
COMPANY_PHONE="+7 (812) 123-45-67"
COMPANY_EMAIL="info@idylle.spb.ru"

# App Configuration
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
NEXT_PUBLIC_APP_NAME="Idylle"
NEXT_PUBLIC_APP_DESCRIPTION="Люксовые парфюмы и товары для дома"
```

### Обязательные переменные для запуска:

- `DATABASE_URL` - строка подключения к PostgreSQL
- `NEXTAUTH_SECRET` - секретный ключ для NextAuth.js (можно сгенерировать: `openssl rand -base64 32`)

## 3. Настройка базы данных

### Создание базы данных PostgreSQL:

```sql
CREATE DATABASE idylle_spb;
```

### Применение миграций:

```bash
npm run db:push
```

Эта команда создаст все таблицы в базе данных согласно схеме Prisma.

## 4. Заполнение тестовыми данными

```bash
npm run db:seed
```

Эта команда создаст:
- Администратора (email: admin@idylle.spb.ru, пароль: admin123)
- Категории товаров
- Бренды
- Тестовые продукты
- Способы оплаты
- Отзывы

## 5. Запуск сервера разработки

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## 6. Дополнительные команды

### Просмотр базы данных:
```bash
npm run db:studio
```

### Сброс базы данных с повторным заполнением:
```bash
npm run db:reset
```

### Генерация Prisma клиента:
```bash
npm run db:generate
```

## 🔑 Доступы

### Админ панель:
- URL: http://localhost:3000/admin
- Email: admin@idylle.spb.ru
- Пароль: admin123

### Пользовательский интерфейс:
- Главная: http://localhost:3000
- Каталог: http://localhost:3000/catalog
- Регистрация: http://localhost:3000/auth/signup
- Вход: http://localhost:3000/auth/signin

## 🛠️ Структура проекта

```
idylle-spb/
├── src/
│   ├── app/                    # Next.js App Router
│   │   ├── api/               # API routes
│   │   ├── auth/              # Страницы аутентификации
│   │   ├── catalog/           # Каталог товаров
│   │   ├── admin/             # Админ панель
│   │   └── globals.css        # Глобальные стили
│   ├── components/            # React компоненты
│   │   ├── ui/               # Базовые UI компоненты
│   │   ├── layout/           # Компоненты макета
│   │   ├── product/          # Компоненты товаров
│   │   └── forms/            # Формы
│   ├── lib/                  # Утилиты и конфигурация
│   └── types/                # TypeScript типы
├── prisma/                   # Схема базы данных
├── scripts/                  # Скрипты
└── public/                   # Статические файлы
```

## 🐛 Решение проблем

### Ошибка подключения к базе данных:
- Проверьте, что PostgreSQL запущен
- Убедитесь, что строка подключения в `.env.local` корректна
- Проверьте, что база данных `idylle_spb` существует

### Ошибки Prisma:
```bash
npm run db:generate
npm run db:push
```

### Ошибки NextAuth:
- Убедитесь, что `NEXTAUTH_SECRET` установлен
- Проверьте, что `NEXTAUTH_URL` соответствует URL приложения

## 📝 Следующие шаги

После успешного запуска вы можете:

1. **Настроить платежные системы** - добавить реальные ключи API
2. **Настроить email** - для уведомлений и рассылки
3. **Добавить реальные товары** - через админ панель
4. **Настроить домен** - для продакшена

## 🆘 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки базы данных
4. Обратитесь к документации Next.js и Prisma
