# 🚀 Деплой на Vercel

## Быстрый деплой

### 1. Установка зависимостей (если еще не установлены)
```bash
npm install
```

### 2. Авторизация в Vercel (если еще не авторизованы)
```bash
vercel login
```

### 3. Подключение проекта к Vercel
```bash
cd /Users/dognev/idylle-spb
vercel
```

Следуйте инструкциям:
- **Set up and deploy?** → `Y`
- **Which scope?** → выберите ваш аккаунт
- **Link to existing project?** → если проект уже существует, выберите `Y`, иначе `N`
- **What's your project's name?** → `idylle-spb`
- **In which directory is your code located?** → `./`

### 4. Продакшн деплой
```bash
vercel --prod
```

## Настройка переменных окружения в Vercel

После деплоя нужно настроить переменные окружения в панели Vercel:

1. Перейдите на https://vercel.com/dashboard
2. Найдите проект `idylle-spb`
3. Откройте **Settings** → **Environment Variables**
4. Добавьте следующие переменные:

### Обязательные переменные:

```env
DATABASE_URL=postgresql://user:password@host:5432/database
NEXTAUTH_SECRET=your-secret-key-here-generate-with-openssl-rand-base64-32
NEXTAUTH_URL=https://idylle-spb.vercel.app
NEXT_PUBLIC_BASE_URL=https://idylle-spb.vercel.app
```

### Рекомендуемые переменные:

```env
# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Payment Providers (опционально)
STRIPE_PUBLISHABLE_KEY=pk_live_...
STRIPE_SECRET_KEY=sk_live_...
YOOKASSA_SHOP_ID=your-shop-id
YOOKASSA_SECRET_KEY=your-secret-key
TINKOFF_TERMINAL_KEY=your-terminal-key
TINKOFF_SECRET_KEY=your-secret-key

# Company Details for Bank Transfer
COMPANY_NAME=ООО Идилль
COMPANY_INN=1234567890
COMPANY_KPP=123456789
COMPANY_BANK_NAME=ПАО Сбербанк
COMPANY_BANK_ACCOUNT=40702810123456789012
COMPANY_BANK_BIK=044525225
COMPANY_BANK_CORR_ACCOUNT=30101810400000000225
COMPANY_ADDRESS=г. Санкт-Петербург, ул. Невский проспект, д. 1
COMPANY_PHONE=+7 (812) 123-45-67
COMPANY_EMAIL=info@idylle.spb.ru

# App Configuration
NEXT_PUBLIC_APP_NAME=Idylle
NEXT_PUBLIC_APP_DESCRIPTION=Люксовые парфюмы и товары для дома
```

### Генерация NEXTAUTH_SECRET:
```bash
openssl rand -base64 32
```

## После деплоя

### 1. Применить миграции базы данных
```bash
# Получить переменные окружения из Vercel
vercel env pull .env.local

# Применить схему к базе данных
npx prisma db push

# Заполнить базу тестовыми данными (опционально)
npm run db:seed
```

### 2. Пересобрать проект после добавления переменных
После добавления переменных окружения в Vercel Dashboard, проект автоматически пересоберется, или можно сделать вручную:

```bash
vercel --prod
```

## Проверка деплоя

1. Откройте https://idylle-spb.vercel.app
2. Проверьте главную страницу
3. Проверьте каталог товаров
4. Проверьте админ-панель (если настроена)

## Автоматический деплой

Если проект подключен к GitHub, Vercel будет автоматически деплоить при каждом пуше в ветку `main`:

```bash
git add .
git commit -m "Деплой на Vercel"
git push origin main
```

## Troubleshooting

### Ошибка подключения к базе данных
- Проверьте, что `DATABASE_URL` правильно настроен в Vercel
- Убедитесь, что база данных доступна из интернета (если используете внешнюю БД)
- Проверьте, что используется Supabase или другая облачная БД для продакшена

### Ошибки сборки
- Проверьте логи в Vercel Dashboard
- Убедитесь, что все зависимости установлены
- Проверьте, что `prisma generate` выполняется во время сборки (настроено в `package.json`)

### Изображения не загружаются
- Проверьте, что домены добавлены в `next.config.js`
- Убедитесь, что пути к изображениям корректны

## Полезные команды

```bash
# Проверить статус деплоя
vercel ls

# Просмотреть логи
vercel logs

# Открыть проект в браузере
vercel open

# Удалить деплой
vercel remove idylle-spb
```

