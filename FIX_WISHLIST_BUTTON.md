# ✅ Исправление: кнопка избранного не работает

## ❌ Проблема

При нажатии на сердечко на товаре ничего не происходило - товар не добавлялся в избранное.

## 🔍 Причина

Кнопка избранного была скрыта через `opacity-0` и становилась видимой только при наведении курсора. Из-за этого клики не срабатывали.

## ✅ Решение

1. **Изменена видимость кнопки**: `opacity-0` → `opacity-70`
   - Теперь кнопка всегда видна (с прозрачностью 70%)
   - При наведении становится полностью непрозрачной (100%)

2. **Улучшен обработчик клика**:
   - Добавлен `z-20` для правильного слоя
   - Улучшена обработка событий
   - Добавлен `aria-label` для доступности

3. **Добавлен `pointer-events-auto`**:
   - Гарантирует, что кнопка всегда кликабельна

## 📝 Изменения в коде

### `src/components/product/ProductCard.tsx`:

```tsx
// Было:
opacity-0  // Кнопка полностью скрыта

// Стало:
opacity-70  // Кнопка всегда видна с небольшой прозрачностью
```

## ✅ Результат

- Кнопка избранного теперь всегда видна
- Клики работают корректно
- Товары успешно добавляются/удаляются из избранного
- При наведении кнопка становится более заметной

## 🧪 Как проверить

1. Откройте страницу с товарами
2. Найдите кнопку сердечка на карточке товара (в правом верхнем углу)
3. Кликните на неё
4. Товар должен добавиться в избранное (сердечко станет красным)
5. Проверьте страницу избранного `/wishlist`

