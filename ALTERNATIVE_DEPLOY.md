# 🚀 Альтернативный деплой сайта

## 📋 Варианты платформ

### Вариант 1: Netlify (БЫСТРЫЙ) ⚡

**Плюсы:**
- ✅ Бесплатный план
- ✅ Отличная поддержка Next.js
- ✅ Автоматический деплой из Git
- ✅ HTTPS по умолчанию

**Минусы:**
- ❌ Нужна внешняя база данных (PostgreSQL)
- ❌ Serverless Functions могут быть медленнее

**Что нужно:**
1. Аккаунт на Netlify (https://netlify.com)
2. База данных PostgreSQL (Railway/Render/Neon)

### Вариант 2: Railway (РЕКОМЕНДУЕТСЯ) 🚂

**Плюсы:**
- ✅ Бесплатный PostgreSQL в одном месте
- ✅ Отличная поддержка Next.js
- ✅ Простой деплой
- ✅ $5 бесплатных кредитов в месяц

**Минусы:**
- ⚠️ После исчерпания кредитов может потребоваться оплата

### Вариант 3: Render 🎨

**Плюсы:**
- ✅ Бесплатный PostgreSQL
- ✅ Хорошая поддержка Next.js
- ✅ Простой деплой

**Минусы:**
- ⚠️ Бесплатные сервисы могут "засыпать" после 15 минут неактивности

### Вариант 4: Fly.io 🪰

**Плюсы:**
- ✅ Отличная производительность
- ✅ Глобальная CDN
- ✅ Поддержка PostgreSQL

**Минусы:**
- ⚠️ Требует больше настройки
- ⚠️ Может быть сложнее для новичков

## 🎯 Рекомендация: Railway

**Почему Railway:**
1. ✅ Все в одном месте (приложение + база данных)
2. ✅ Бесплатный PostgreSQL
3. ✅ Простой деплой
4. ✅ Автоматический HTTPS

---

## 🚀 Деплой на Railway (Пошаговая инструкция)

### Шаг 1: Создать аккаунт на Railway

1. Перейдите на https://railway.app
2. Нажмите "Start a New Project" или "Login"
3. Войдите через GitHub (рекомендуется)

### Шаг 2: Создать новый проект

1. Нажмите "+ New Project"
2. Выберите "Deploy from GitHub repo"
3. Подключите репозиторий с проектом `idylle-spb`
4. Или выберите "Empty Project" и потом подключите репозиторий

### Шаг 3: Добавить PostgreSQL базу данных

1. В проекте Railway нажмите "+ New"
2. Выберите "Database" → "Add PostgreSQL"
3. Railway создаст базу данных и автоматически добавит переменную `DATABASE_URL`

### Шаг 4: Настроить переменные окружения

1. В проекте Railway нажмите на ваше приложение
2. Перейдите в раздел "Variables"
3. Добавьте следующие переменные:

```env
DATABASE_URL=автоматически добавлено Railway

NEXTAUTH_SECRET=uvZuO6y91hb2zbxwY3EmtqKH9YeJSyT58AH5nMR3KWY=
NEXTAUTH_URL=https://your-app-name.up.railway.app
NEXT_PUBLIC_BASE_URL=https://your-app-name.up.railway.app

NEXT_PUBLIC_APP_NAME=Idylle
NEXT_PUBLIC_APP_DESCRIPTION=Люксовые парфюмы и товары для дома

# Опционально (если нужно)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
VK_CLIENT_ID=
VK_CLIENT_SECRET=
```

**Важно:** 
- `DATABASE_URL` Railway добавит автоматически
- `NEXTAUTH_URL` и `NEXT_PUBLIC_BASE_URL` - замените на URL вашего приложения Railway
- URL будет в формате: `https://idylle-spb-production.up.railway.app`

### Шаг 5: Настроить деплой

1. Railway автоматически обнаружит Next.js проект
2. Настройки Build:
   - **Build Command:** `prisma generate && next build`
   - **Start Command:** `next start`
   - **Root Directory:** `/` (по умолчанию)

3. Railway автоматически начнет деплой после подключения репозитория

### Шаг 6: Применить миграции базы данных

После первого деплоя нужно применить схему базы данных:

**Вариант A: Через Railway CLI (рекомендуется)**

1. Установите Railway CLI:
   ```bash
   npm i -g @railway/cli
   ```

2. Войдите в Railway:
   ```bash
   railway login
   ```

3. Подключитесь к проекту:
   ```bash
   railway link
   ```

4. Примените миграции:
   ```bash
   railway run npx prisma db push
   ```

5. (Опционально) Заполните тестовыми данными:
   ```bash
   railway run npm run db:seed
   ```

**Вариант B: Через Railway Dashboard**

1. В проекте Railway откройте ваше приложение
2. Перейдите в раздел "Deployments"
3. Нажмите на последний деплой
4. Откройте "Shell" или "Logs"
5. Выполните команды в терминале

### Шаг 7: Получить URL приложения

1. В Railway Dashboard откройте ваш проект
2. Нажмите на приложение (не на базу данных)
3. Найдите раздел "Settings" → "Domains"
4. Там будет URL вашего приложения (например: `https://idylle-spb-production.up.railway.app`)

### Шаг 8: Обновить переменные окружения

После получения URL обновите переменные:

1. В Railway Dashboard → Variables
2. Обновите:
   - `NEXTAUTH_URL` = `https://your-app-name.up.railway.app`
   - `NEXT_PUBLIC_BASE_URL` = `https://your-app-name.up.railway.app`
3. Railway автоматически пересоберет приложение

## ✅ После деплоя

1. **Проверьте сайт:** Откройте URL из Railway
2. **Проверьте логин:** Попробуйте войти в админ-панель
3. **Примените миграции:** Если еще не применили (см. Шаг 6)
4. **Заполните данными:** Если нужно (см. Шаг 6)

---

## 🔄 Быстрый деплой на Netlify

### Шаг 1: Создать Netlify аккаунт

1. Перейдите на https://netlify.com
2. Войдите через GitHub

### Шаг 2: Подключить репозиторий

1. Нажмите "Add new site" → "Import an existing project"
2. Подключите репозиторий `idylle-spb`

### Шаг 3: Настроить деплой

Netlify автоматически обнаружит Next.js, но нужно настроить:

**Build settings:**
- **Build command:** `prisma generate && next build`
- **Publish directory:** `.next`
- **Base directory:** `/`

**⚠️ Важно:** Netlify может потребовать дополнительные настройки для Next.js

### Шаг 4: Добавить переменные окружения

1. Перейдите в Site settings → Environment variables
2. Добавьте все необходимые переменные (см. выше)

**⚠️ Проблема:** Нужна внешняя база данных PostgreSQL (Railway/Render/Neon)

---

## 📝 Создание конфигурационного файла для Railway

Создам файл `railway.json` для автоматической настройки:

