# ✅ Следующие шаги: Деплой на Railway

## 🎉 Что уже сделано

- ✅ Код успешно загружен на GitHub
- ✅ Репозиторий создан: `https://github.com/ognewd/idylle-spb`
- ✅ Все большие файлы удалены из Git
- ✅ `.gitignore` настроен правильно

## 🚀 Что делать дальше

### Шаг 1: Вернуться в Railway

1. Откройте: https://railway.app/new/github
2. Обновите страницу (F5) если нужно
3. Репозиторий `idylle-spb` должен появиться в списке
4. Нажмите на репозиторий `idylle-spb` для деплоя

### Шаг 2: Railway автоматически начнет деплой

Railway автоматически:
- Обнаружит Next.js проект
- Начнет сборку
- Запустит деплой

Вы увидите процесс деплоя в реальном времени.

### Шаг 3: Добавить PostgreSQL базу данных

**ВАЖНО:** Это нужно сделать во время деплоя или сразу после:

1. В проекте Railway нажмите **"+ New"** (вверху справа)
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически:
   - Создаст PostgreSQL базу данных
   - Добавит переменную `DATABASE_URL` в ваше приложение
   - Перезапустит приложение с новой переменной

### Шаг 4: Настроить переменные окружения

1. В проекте Railway нажмите на ваше **приложение** (не на базу данных)
2. Перейдите в раздел **"Variables"** (вкладка вверху)
3. Добавьте переменные (кнопка **"+ New Variable"**):

```
NEXTAUTH_SECRET=uvZuO6y91hb2zbxwY3EmtqKH9YeJSyT58AH5nMR3KWY=
NEXT_PUBLIC_APP_NAME=Idylle
NEXT_PUBLIC_APP_DESCRIPTION=Люксовые парфюмы и товары для дома
```

**Примечание:** `DATABASE_URL` Railway добавит автоматически после создания базы данных!

### Шаг 5: Получить URL приложения

1. В проекте Railway нажмите на ваше **приложение**
2. Перейдите в **"Settings"** (вкладка)
3. Найдите раздел **"Domains"**
4. Там будет URL вашего приложения (например: `https://idylle-spb-production-xxxx.up.railway.app`)
5. **Скопируйте этот URL**

### Шаг 6: Обновить NEXTAUTH_URL

1. Вернитесь в **"Variables"** вашего приложения
2. Добавьте еще две переменные:

```
NEXTAUTH_URL=https://your-app-name.up.railway.app
NEXT_PUBLIC_BASE_URL=https://your-app-name.up.railway.app
```

**Важно:** Замените `your-app-name` на реальный URL из шага 5!

3. Railway автоматически пересоберет приложение

### Шаг 7: Применить миграции базы данных

После получения URL и обновления переменных:

**Способ 1: Через Railway CLI (рекомендуется)**

```bash
# Установить Railway CLI (если еще не установлен)
npm i -g @railway/cli

# Войти в Railway
railway login

# Подключиться к проекту
cd /Users/dognev/idylle-spb
railway link
# Выберите ваш проект из списка

# Применить схему базы данных
railway run npx prisma db push

# (Опционально) Заполнить тестовыми данными
railway run npm run db:seed
```

**Способ 2: Через Railway Dashboard**

1. В проекте Railway откройте ваше **приложение**
2. Перейдите в **"Deployments"** (вкладка)
3. Найдите последний деплой
4. Откройте **"Shell"** (если доступен)
5. Выполните команды в терминале:
   ```bash
   npx prisma db push
   ```

## ✅ Проверка после деплоя

После выполнения всех шагов:

1. **Откройте URL** вашего приложения из Railway
2. **Проверьте главную страницу** - должна загрузиться
3. **Попробуйте войти** в админ-панель (`/admin/login`)

## 🔍 Если что-то не работает

1. **Проверьте логи:**
   - Railway Dashboard → Deployments → Logs
   - Ищите ошибки в логах

2. **Проверьте переменные окружения:**
   - Убедитесь, что все переменные добавлены
   - Проверьте, что `DATABASE_URL` есть (добавляется автоматически)

3. **Проверьте миграции:**
   - Убедитесь, что `npx prisma db push` выполнен успешно

## 📝 Важные замечания

- ⚠️ **База данных обязательна** - без нее приложение не запустится
- ⚠️ **NEXTAUTH_URL обязателен** - без него авторизация не будет работать
- ✅ **DATABASE_URL добавляется автоматически** - после создания PostgreSQL
- ✅ **Railway автоматически пересобирает** приложение после изменения переменных

## 🎯 Готово!

После выполнения всех шагов ваш сайт будет доступен в интернете по URL из Railway! 🚀

