# 📍 ГДЕ ВЗЯТЬ ФАЙЛЫ КОМПОНЕНТОВ

## ✅ Все файлы находятся в папке `/EXPORT_READY_COMPONENTS/`

---

## 🎯 Самый простой способ:

### **1. Откройте папку `/EXPORT_READY_COMPONENTS/` в этом проекте**

Вы увидите 7 файлов:

```
/EXPORT_READY_COMPONENTS/
├── README.md                 ← Инструкция
├── Header.tsx                ← Шапка сайта
├── HeroSection.tsx           ← Главная секция со слайдером ⭐
├── FeaturesSection.tsx       ← Преимущества
├── CategoriesSection.tsx     ← Категории
├── ProductGallery.tsx        ← Галерея продуктов
└── Footer.tsx                ← Подвал
```

---

## 📋 Пошаговая инструкция:

### **Шаг 1: Откройте два окна Cursor рядом**

```
┌─────────────────────┐  ┌─────────────────────┐
│  Этот проект        │  │  Ваш проект         │
│  (Figma Make)       │  │                     │
│                     │  │                     │
│  /EXPORT_READY_     │  │  src/               │
│   COMPONENTS/       │  │   components/       │
│   ├── Header.tsx    │→ │   ├── (пусто)       │
│   ├── HeroSection   │→ │   └── ...           │
│   └── ...           │  │                     │
└─────────────────────┘  └─────────────────────┘
```

### **Шаг 2: Перетащите файлы**

Из **левого** окна (этот проект):
- `/EXPORT_READY_COMPONENTS/Header.tsx`
- `/EXPORT_READY_COMPONENTS/HeroSection.tsx`  
- `/EXPORT_READY_COMPONENTS/FeaturesSection.tsx`
- `/EXPORT_READY_COMPONENTS/CategoriesSection.tsx`
- `/EXPORT_READY_COMPONENTS/ProductGallery.tsx`
- `/EXPORT_READY_COMPONENTS/Footer.tsx`

В **правое** окно (ваш проект):
- `src/components/`

---

## 🔍 Альтернатива: Copy-Paste

Если не получается перетащить:

### 1. Откройте `/EXPORT_READY_COMPONENTS/Header.tsx`
### 2. Скопируйте весь код (Cmd/Ctrl + A, затем Cmd/Ctrl + C)
### 3. В вашем проекте создайте `src/components/Header.tsx`
### 4. Вставьте код (Cmd/Ctrl + V)
### 5. Повторите для остальных 5 файлов

---

## 📦 После копирования:

### 1. Установите зависимости:
```bash
npm install react-slick slick-carousel lucide-react
```

### 2. Создайте файл стилей:

**Путь:** `src/styles/animations.css`

**Содержимое:** (скопируйте из `/EXPORT_READY_COMPONENTS/README.md` раздел "Создайте файл стилей")

### 3. Обновите App.tsx:

**Содержимое:** (скопируйте из `/EXPORT_READY_COMPONENTS/README.md` раздел "Обновите App.tsx")

---

## ✅ Готовые файлы

Все файлы в `/EXPORT_READY_COMPONENTS/` **уже готовы к использованию**:

- ✅ Без зависимостей от Figma Make
- ✅ Без импортов `figma:asset`
- ✅ Только чистый React + Tailwind
- ✅ Работают сразу после копирования

---

## 🎨 Что внутри каждого файла:

### `Header.tsx`
```tsx
import { Search, Heart, ShoppingCart, User, Phone, MapPin } from "lucide-react";

export function Header() {
  // Шапка с поиском, навигацией, корзиной
}
```

### `HeroSection.tsx` ⭐
```tsx
import { MapPin, ShoppingBag, ArrowRight } from "lucide-react";
import Slider from "react-slick";

export function HeroSection() {
  // Главная секция с а��томатическим слайдером товаров
}
```

### `FeaturesSection.tsx`
```tsx
import { Truck, MapPin, Gift } from "lucide-react";

export function FeaturesSection() {
  // Секция преимуществ с золотыми иконками
}
```

### `CategoriesSection.tsx`
```tsx
import { ArrowRight } from "lucide-react";

export function CategoriesSection() {
  // 3 категории с изображениями и градиентами
}
```

### `ProductGallery.tsx`
```tsx
import { ArrowRight } from "lucide-react";

export function ProductGallery() {
  // Галерея из 6 продуктов в сетке 3×2
}
```

### `Footer.tsx`
```tsx
import { Facebook, Instagram, Twitter, Youtube, Mail, Phone, MapPin, Clock } from "lucide-react";

export function Footer() {
  // Подвал с контактами, ссылками, соцсетями
}
```

---

## 🚀 Быстрый старт через Cursor AI

Если лень копировать вручную:

1. Откройте Cursor Chat (**Cmd/Ctrl + L**)
2. Прикрепите папку `/EXPORT_READY_COMPONENTS/`
3. Напишите:

```
Скопируй все .tsx файлы из прикрепленной папки в src/components/ моего проекта.
Также создай файл animations.css и обнови App.tsx как указано в README.md.
```

Cursor сделает всё автоматически! 🤖

---

## 🎯 Структура после копирования:

```
ваш-проект/
├── src/
│   ├── components/           ✅ Скопированы
│   │   ├── Header.tsx       
│   │   ├── HeroSection.tsx  
│   │   ├── FeaturesSection.tsx
│   │   ├── CategoriesSection.tsx
│   │   ├── ProductGallery.tsx
│   │   └── Footer.tsx
│   │
│   ├── styles/              ✅ Создать
│   │   └── animations.css   
│   │
│   └── App.tsx              ✅ Обновить
│
└── package.json             ✅ npm install
```

---

## 📞 Нужна помощь?

### Проблема: "Не вижу папку /EXPORT_READY_COMPONENTS/"

**Решение:** Обновите файловое дерево в Cursor (нажмите F5 в панели файлов)

### Проблема: "Файлы не открываются"

**Решение:** Кликните правой кнопкой → "Открыть в редакторе"

### Проблема: "Не получается перетащить"

**Решение:** Используйте метод Copy-Paste (описан выше)

---

## 🎉 Готово!

Теперь вы знаете, где взять файлы и как их использовать.

**Следующий шаг:** Откройте `/EXPORT_READY_COMPONENTS/README.md` для детальных инструкций по установке.

---

**Путь к файлам:** `/EXPORT_READY_COMPONENTS/` ← Здесь всё! ✨
