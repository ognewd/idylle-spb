# ✅ Решение проблемы "Всего товаров: 0" в локальной админ-панели

## 📊 Текущая ситуация

- ✅ В локальной БД есть **1045 товаров**
- ✅ В БД есть **2 администратора**
- ❌ Админ-панель показывает "Всего товаров: 0"

## 🔍 Причина

Проблема в **авторизации**. API `/api/admin/products` требует Bearer token, и запрос возвращает ошибку 401 (Unauthorized) или пустой результат.

## ✅ Решение

### 1. Проверьте авторизацию

1. **Откройте консоль браузера** (F12)
2. Перейдите во вкладку **Network**
3. Обновите страницу `/admin/products`
4. Найдите запрос к `/api/admin/products`
5. Проверьте:
   - Статус ответа (200, 401, 500?)
   - Заголовки запроса (есть ли `Authorization: Bearer ...`)
   - Ответ сервера (ошибка или данные)

### 2. Войдите заново

1. Выйдите из админ-панели (кнопка "Выйти")
2. Откройте `/admin/login`
3. Войдите:
   - Email: `admin@idylle.spb.ru`
   - Пароль: `admin123`

### 3. Проверьте токен

В консоли браузера выполните:

```javascript
// Проверьте токен
const token = localStorage.getItem('admin_token');
console.log('Токен:', token ? 'Есть' : 'Нет');

// Если нет токена - нужно войти заново
```

### 4. Если проблема сохраняется

Проверьте переменную окружения `NEXTAUTH_SECRET`:

1. Убедитесь, что в `.env.local` есть:
   ```
   NEXTAUTH_SECRET=your-secret-key
   ```

2. Перезапустите dev сервер:
   ```bash
   # Остановить (Ctrl+C)
   # Запустить снова
   npm run dev
   ```

## 🎯 Ожидаемый результат

После входа в админ-панель должны отображаться все 1045 товаров.

## 📝 Быстрая проверка

1. Откройте http://localhost:3000/admin/login
2. Войдите как администратор
3. Перейдите в http://localhost:3000/admin/products
4. Должны отображаться товары

