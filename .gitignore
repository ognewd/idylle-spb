.vercel
node_modules
.next
.DS_Store
# Env-файлы с секретами (НЕ коммитить)
.env
.env.local
.env*.local
.env.production
*.log

# Загруженные файлы (не должны быть в Git)
public/uploads/

# Временные файлы
остатки и ШК.xls
*.xls
*.xlsx
!data/*.csv

# Build artifacts
*.pack.gz
.next/cache/

# Database backups (should not be in git)
*_backup.sql
*_data_only.sql
idylle_spb_backup.sql
idylle_spb_data_only.sql
prod_dump.sql

# Env backups (never commit)
.env*.bak
.env*.backup
.env.production.backup

# Supabase (не используется)
.supabase*

# PM2 ecosystem config (содержит секреты)
ecosystem.config.cjs

# TLS-сертификаты платёжного шлюза БСПБ
certs/
