# 🚀 Команды для пуша кода на GitHub

## ✅ Что уже сделано

- ✅ Обновлен `.gitignore` (исключены `.next`, `.env.local`, `.DS_Store`)
- ✅ Обновлен remote URL на: `https://github.com/ognewd/idylle-spb.git`
- ✅ GitHub username: `ognewd`

## 📋 Команды для выполнения

**После того, как вы создадите репозиторий на GitHub** (нажали "Create repository"), выполните:

```bash
cd /Users/dognev/idylle-spb

# Добавить все файлы (исключая .gitignore)
git add .

# Закоммитить изменения
git commit -m "Initial commit: Railway deployment setup"

# Убедиться, что ветка называется main
git branch -M main

# Запушить код на GitHub
git push -u origin main
```

## ⚠️ Если будет ошибка авторизации

Если Git попросит авторизацию, используйте один из способов:

### Способ 1: Personal Access Token (Рекомендуется)

1. Создайте токен на GitHub:
   - Откройте: https://github.com/settings/tokens
   - Нажмите "Generate new token" → "Generate new token (classic)"
   - Название: `Railway Deployment`
   - Выберите `repo` (Full control of private repositories)
   - Нажмите "Generate token"
   - **Скопируйте токен** (показывается только один раз!)

2. При пуше используйте токен как пароль:
   ```bash
   Username: ognewd
   Password: [вставить токен]
   ```

### Способ 2: GitHub CLI

```bash
# Установить GitHub CLI (если не установлен)
brew install gh

# Авторизоваться
gh auth login

# Затем пушить как обычно
git push -u origin main
```

## ✅ После успешного пуша

1. **Проверьте на GitHub:**
   - Откройте https://github.com/ognewd/idylle-spb
   - Убедитесь, что все файлы загружены

2. **Вернитесь в Railway:**
   - Обновите страницу Railway (F5)
   - Репозиторий `idylle-spb` должен появиться в списке
   - Нажмите на него для деплоя

## 🎯 Готово!

После пуша кода Railway автоматически увидит репозиторий, и вы сможете начать деплой!

