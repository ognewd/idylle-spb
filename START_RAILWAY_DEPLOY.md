# 🚂 Запуск деплоя на Railway

## ✅ Что уже готово

- ✅ Проект в Git репозитории
- ✅ Конфигурационные файлы созданы (`railway.json`)
- ✅ Инструкции подготовлены

## 🚀 Быстрый старт (5 минут)

### 1. Откройте Railway

Перейдите на: **https://railway.app**

### 2. Войдите через GitHub

1. Нажмите "Login with GitHub"
2. Авторизуйтесь через GitHub
3. Подтвердите доступ Railway к вашим репозиториям

### 3. Создайте новый проект

1. Нажмите **"+ New Project"** (зеленая кнопка)
2. Выберите **"Deploy from GitHub repo"**
3. Найдите репозиторий `idylle-spb`
4. Нажмите на него
5. Railway автоматически начнет деплой

### 4. Добавьте PostgreSQL базу данных

1. В проекте Railway нажмите **"+ New"** (вверху справа)
2. Выберите **"Database"** → **"Add PostgreSQL"**
3. Railway автоматически:
   - Создаст PostgreSQL базу данных
   - Добавит переменную `DATABASE_URL` в ваше приложение

### 5. Настройте переменные окружения

1. В проекте Railway нажмите на ваше **приложение** (не на базу данных)
2. Перейдите в раздел **"Variables"** (вкладка вверху)
3. Добавьте переменные (кнопка **"+ New Variable"**):

```
NEXTAUTH_SECRET=uvZuO6y91hb2zbxwY3EmtqKH9YeJSyT58AH5nMR3KWY=
NEXT_PUBLIC_APP_NAME=Idylle
NEXT_PUBLIC_APP_DESCRIPTION=Люксовые парфюмы и товары для дома
```

**Примечание:** `DATABASE_URL` уже добавлен автоматически!

### 6. Получите URL приложения

1. В проекте Railway нажмите на ваше **приложение**
2. Перейдите в **"Settings"** (вкладка)
3. Найдите раздел **"Domains"**
4. Там будет URL вашего приложения (например: `https://idylle-spb-production-xxxx.up.railway.app`)
5. **Скопируйте этот URL**

### 7. Обновите NEXTAUTH_URL

1. Вернитесь в **"Variables"**
2. Добавьте еще две переменные:

```
NEXTAUTH_URL=https://your-app-name.up.railway.app
NEXT_PUBLIC_BASE_URL=https://your-app-name.up.railway.app
```

**Важно:** Замените `your-app-name` на реальный URL из шага 6!

3. Railway автоматически пересоберет приложение

### 8. Примените миграции базы данных

**Способ 1: Через Railway CLI (рекомендуется)**

```bash
# Установить Railway CLI
npm i -g @railway/cli

# Войти в Railway
railway login

# Подключиться к проекту
cd /Users/dognev/idylle-spb
railway link
# Выберите ваш проект из списка

# Применить схему базы данных
railway run npx prisma db push

# (Опционально) Заполнить тестовыми данными
railway run npm run db:seed
```

**Способ 2: Через Railway Dashboard**

1. В проекте Railway откройте ваше **приложение**
2. Перейдите в **"Deployments"** (вкладка)
3. Нажмите на **последний деплой**
4. Откройте **"Logs"** или **"Shell"** (если доступен)
5. Выполните команды в терминале:
   ```bash
   npx prisma db push
   ```

## ✅ Готово!

После выполнения всех шагов:

1. **Откройте URL** вашего приложения из Railway
2. **Проверьте главную страницу**
3. **Попробуйте войти** в админ-панель

## 🔍 Что делать дальше

- ✅ Сайт будет доступен в интернете по URL из Railway
- ✅ База данных PostgreSQL работает
- ✅ HTTPS включен автоматически
- ✅ Автоматический деплой при пуше в GitHub

## 🆘 Если что-то не работает

1. **Проверьте логи** в Railway Dashboard → Deployments → Logs
2. **Проверьте переменные окружения** в Variables
3. **Убедитесь**, что миграции применены (`npx prisma db push`)

## 📝 Полезные ссылки

- Railway Dashboard: https://railway.app/dashboard
- Railway Docs: https://docs.railway.app
- Railway CLI: https://docs.railway.app/develop/cli

