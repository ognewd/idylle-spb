# ⚠️ Проблема: Техническое обслуживание Supabase

## 🔍 Обнаруженная проблема

На странице интеграции Supabase в Vercel видно:

**⚠️ ЖЕЛТОЕ ПРЕДУПРЕЖДЕНИЕ:**
> "Supabase Maintenance: Dashboard and Management API will experience downtime Nov 21-23, 2025"

Это означает, что **Supabase находится на техническом обслуживании**, что объясняет ошибку "Resource provisioning failed" в Vercel.

## ❌ Что происходит

1. Vercel пытается подготовить интеграцию Supabase во время деплоя
2. Supabase API недоступен из-за технического обслуживания (Nov 21-23, 2025)
3. Vercel не может получить доступ к ресурсам Supabase
4. Деплой завершается с ошибкой "Resource provisioning failed"

## ✅ Решение

### Вариант 1: Подождать окончания обслуживания (Рекомендуется)

**Дата окончания:** 23 ноября 2025

После окончания технического обслуживания:
1. Supabase API снова станет доступен
2. Vercel сможет подготовить ресурсы
3. Деплои начнут проходить успешно

**Попробуйте деплой после 23 ноября 2025:**
```bash
cd /Users/dognev/idylle-spb
vercel --prod
```

### Вариант 2: Использовать прямое подключение через DATABASE_URL (Временное решение)

Если нужно задеплоить сейчас:

1. **Убедитесь, что `DATABASE_URL` настроен** в Environment Variables с полной строкой подключения к Supabase
2. **Удалите интеграцию Supabase** из Vercel (временно):
   - Перейдите на https://vercel.com/dognevs-projects/idylle-spb/settings/integrations
   - Найдите Supabase
   - Нажмите "Manage" → "Remove" или "Uninstall"
3. **Запустите деплой:**
   ```bash
   cd /Users/dognev/idylle-spb
   vercel --prod
   ```

**ВАЖНО:** После окончания обслуживания Supabase можно будет переподключить интеграцию.

### Вариант 3: Проверить статус Supabase

1. Перейдите на https://status.supabase.com
2. Проверьте текущий статус служб Supabase
3. Убедитесь, что обслуживание действительно идет

## 📝 Почему это проблема

**Техническое обслуживание Supabase влияет на:**
- Dashboard API - недоступен
- Management API - недоступен
- Интеграции Vercel - не могут подготовить ресурсы

**Но НЕ влияет на:**
- Работающие подключения к базе данных через `DATABASE_URL`
- Существующие проекты и данные
- Приложения, которые уже запущены

## ✅ Текущий статус

**Хорошие новости:**
- ✅ Старый деплой продолжает работать (использует прямое подключение к БД)
- ✅ Сайт доступен на https://idylle-spb.vercel.app
- ✅ База данных работает нормально

**Проблема только в:**
- ❌ Новых деплоях (не могут подготовить интеграцию Supabase)
- ❌ Обновлении интеграции через Vercel

## 🎯 Рекомендация

**Действия:**
1. **Подождать окончания обслуживания** (до 23 ноября 2025)
2. **После окончания** попробовать новый деплой:
   ```bash
   vercel --prod
   ```
3. **Если проблема останется**, удалить интеграцию Supabase и использовать только `DATABASE_URL`

**Или если нужно задеплоить сейчас:**
- Удалить интеграцию Supabase из Vercel
- Использовать только `DATABASE_URL` (который уже настроен)
- После окончания обслуживания можно переподключить интеграцию

## ⚠️ Важно

**Старый деплой работает нормально**, что означает:
- ✅ Код исправлен
- ✅ База данных доступна через `DATABASE_URL`
- ✅ Все настройки правильные

Проблема только в процессе подготовки ресурсов через интеграцию Vercel во время технического обслуживания Supabase.

