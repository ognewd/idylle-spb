# 🔧 Решение ошибки "Resource provisioning failed"

## 🔍 Проблема

Ошибка "Resource provisioning failed" в Vercel означает, что Vercel не может подготовить ресурсы перед сборкой. Это часто связано с:

1. **Интеграциями Vercel** - проблема с подключением к внешним сервисам (Supabase, и т.д.)
2. **Переменными окружения** - конфликт или неверная настройка
3. **Лимитами ресурсов** - превышение лимитов Vercel или внешних сервисов

## ✅ Решение

### Вариант 1: Проверить интеграцию Supabase в Vercel

1. Перейдите на https://vercel.com/dognevs-projects/idylle-spb/settings/integrations
2. Проверьте, есть ли интеграция Supabase
3. Если интеграция есть, попробуйте:
   - Отключить и переподключить интеграцию
   - Проверить, что подключение активно

### Вариант 2: Упростить переменные окружения

У вас много переменных для базы данных. Убедитесь, что `DATABASE_URL` настроен правильно и содержит полную строку подключения к Supabase.

**Проверьте в Vercel Dashboard:**
- https://vercel.com/dognevs-projects/idylle-spb/settings/environment-variables
- Убедитесь, что `DATABASE_URL` для Production имеет правильный формат:
  ```
  postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres?pgbouncer=true
  ```

### Вариант 3: Удалить лишние переменные (если они не используются)

Если проект использует только `DATABASE_URL`, можно удалить лишние переменные:
- `POSTGRES_URL`
- `POSTGRES_PRISMA_URL`
- `POSTGRES_URL_NON_POOLING`
- `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DATABASE`, `POSTGRES_HOST`
- `SUPABASE_*` переменные (если не используются напрямую)

**ВАЖНО:** Удаляйте переменные только если вы уверены, что они не используются!

### Вариант 4: Проверить лимиты Supabase

1. Перейдите на https://supabase.com/dashboard
2. Откройте проект `idylle-spb`
3. Проверьте, не исчерпаны ли лимиты:
   - Database connections
   - Project quota
   - Storage quota

### Вариант 5: Использовать только DATABASE_URL

В `src/lib/prisma.ts` используется только `DATABASE_URL`. Убедитесь, что:
- `DATABASE_URL` содержит правильную строку подключения
- Формат: `postgresql://postgres:[password]@db.[project].supabase.co:5432/postgres`

## 🚀 Быстрое решение

Попробуйте:

1. **Удалить интеграцию Supabase из Vercel** (если она есть)
2. **Оставить только `DATABASE_URL`** с полной строкой подключения
3. **Пересобрать проект**: `vercel --prod`

## 📝 Проверка после исправления

После исправления проверьте:
1. Новый деплой запускается (не "Resource provisioning failed")
2. Сайт работает: https://idylle-spb.vercel.app
3. Подключение к базе данных работает

## ⚠️ Примечание

Если старый деплой работает нормально, это означает, что проблема именно в процессе деплоя, а не в самом приложении.

