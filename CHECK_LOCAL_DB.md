# 🔍 Проверка локальной БД

## ❌ Проблема

В админ-панели показывается "Всего товаров: 0" и "Нет товаров".

## 🔍 Возможные причины

1. **Локальная БД пустая** - нет товаров в локальной базе данных
2. **БД не подключена** - проблемы с подключением к локальной БД
3. **Товары только в продакшене** - товары есть только в продакшен БД

## ✅ Что проверить

### 1. Подключение к БД

Локальная БД должна быть:
- `postgresql://dognev@localhost:5432/idylle_spb`
- Или `postgresql://dognev@localhost:5432/idylle_spb_dev`

### 2. Проверка товаров в БД

Выполните в терминале:

```bash
# Вариант 1: Через Prisma Studio (GUI)
npm run db:studio

# Вариант 2: Через psql
psql -h localhost -U dognev -d idylle_spb -c "SELECT COUNT(*) FROM products;"

# Вариант 3: Через скрипт
node -e "const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); prisma.product.count().then(count => { console.log('Товаров:', count); prisma.\$disconnect(); });"
```

### 3. Если товаров нет

Варианты:
- Добавить товары вручную через админ-панель
- Импортировать из Excel
- Заполнить тестовыми данными через seed скрипт

## 🔧 Решение

1. Проверьте подключение к локальной БД
2. Если БД пустая - добавьте товары или заполните тестовыми данными
3. Если БД не подключена - проверьте DATABASE_URL в .env.local

