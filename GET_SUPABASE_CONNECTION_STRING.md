# 🔧 Как получить Connection Pooling URL для Supabase

## 📋 Пошаговая инструкция

### Шаг 1: Открыть Supabase Dashboard

1. Перейдите на **https://supabase.com/dashboard**
2. Войдите в свой аккаунт
3. Найдите и откройте проект **`idylle-spb`** (или ваш проект)

### Шаг 2: Перейти в настройки базы данных

1. В левом боковом меню найдите **"Settings"** (⚙️ Настройки)
2. Нажмите на **"Settings"**
3. В подменю выберите **"Database"** (🗄️ База данных)

### Шаг 3: Найти Connection String

На странице Database Settings вы увидите несколько секций:

#### Вариант A: Connection Pooling (РЕКОМЕНДУЕТСЯ для продакшена)

1. Найдите секцию **"Connection Pooling"** или **"Connection string"**
2. Вы увидите несколько режимов:
   - **Session mode** (рекомендуется для приложений)
   - **Transaction mode**
   - **Statement mode**
3. Выберите **"Session mode"** (это для connection pooling)
4. Нажмите на кнопку **"Copy"** или скопируйте строку подключения

**Формат будет выглядеть примерно так:**
```
postgresql://postgres.abcdefghijklmnop:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true
```

#### Вариант B: Direct Connection (для миграций, НЕ для продакшена)

Если не найдете Connection Pooling:
1. Найдите секцию **"Connection string"** или **"Connection info"**
2. Выберите формат **"URI"**
3. Скопируйте строку подключения

**Формат будет выглядеть так:**
```
postgresql://postgres:[YOUR-PASSWORD]@db.abcdefghijklmnop.supabase.co:5432/postgres
```

⚠️ **ВНИМАНИЕ:** Для продакшена НЕ используйте прямой URL - только Connection Pooling URL!

### Шаг 4: Скопировать Connection String

1. Нажмите на кнопку **"Copy"** рядом со строкой подключения
2. Или выделите текст и скопируйте (Ctrl+C / Cmd+C)

**ВАЖНО:** 
- Строка подключения содержит пароль - не делитесь ей публично!
- Убедитесь, что скопировали **всю** строку, включая параметр `?pgbouncer=true`

### Шаг 5: Обновить DATABASE_URL в Vercel

1. Перейдите на **https://vercel.com/dognevs-projects/idylle-spb/settings/environment-variables**
2. Найдите переменную **`DATABASE_URL`** в списке
3. Нажмите на **✏️ (редактировать)** рядом с переменной для Production
4. Вставьте скопированный Connection Pooling URL из Supabase
5. Убедитесь, что выбран **Production** environment
6. Нажмите **"Save"** (Сохранить)

### Шаг 6: Проверить формат

После вставки проверьте, что строка содержит:

✅ **Должно быть:**
- `postgres.` в начале (не просто `postgres:`)
- `.pooler.supabase.com` в домене
- Порт `6543` (для connection pooling) или `5432`
- `?pgbouncer=true` в конце

❌ **НЕ должно быть:**
- `db.[project].supabase.co` (это прямой URL, не для продакшена)
- Порт `5432` без `?pgbouncer=true` (для connection pooling нужен `6543`)

### Примеры правильных форматов:

#### ✅ Правильный формат (Connection Pooling):
```
postgresql://postgres.abcdefghijklmnop:your-password@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?pgbouncer=true
```

#### ❌ Неправильный формат (Direct Connection - не для продакшена):
```
postgresql://postgres:your-password@db.abcdefghijklmnop.supabase.co:5432/postgres
```

## 🔍 Как проверить, что формат правильный

### Визуальная проверка:

**Connection Pooling URL должен содержать:**
1. `postgres.` (с точкой после postgres)
2. `.pooler.supabase.com` в домене
3. Порт `6543` (или `5432` с `?pgbouncer=true`)
4. `?pgbouncer=true` в конце

**Прямой URL (не использовать для продакшена) содержит:**
1. `postgres:` (без точки)
2. `db.[project].supabase.co` в домене
3. Порт `5432`
4. Нет параметра `?pgbouncer=true`

## 📝 Альтернативный способ: Проверить в Supabase

Если не можете найти Connection Pooling URL:

1. Перейдите в **Settings** → **Database**
2. Посмотрите секцию **"Connection Pooling"**
3. Если секции нет, возможно:
   - Нужно включить Connection Pooling (обычно включено по умолчанию)
   - Или используйте прямую строку, но добавьте параметры для pooling

## ⚠️ Важные замечания

1. **Не используйте прямой URL на продакшене** - это может привести к исчерпанию лимитов соединений
2. **Connection Pooling обязателен** для Vercel Serverless Functions
3. **Пароль в URL** - убедитесь, что не делитесь им публично
4. **После обновления** переменной Vercel может автоматически пересобрать проект

## ✅ После обновления

После обновления `DATABASE_URL` в Vercel:

1. **Подождите несколько минут** (Vercel может пересобрать проект автоматически)
2. Или **запустите деплой вручную:**
   ```bash
   cd /Users/dognev/idylle-spb
   vercel --prod
   ```
3. **Проверьте логин** - ошибка "Tenant or user not found" должна исчезнуть

## 🆘 Если Connection Pooling URL не доступен

Если в Supabase Dashboard нет Connection Pooling URL:

1. Проверьте, что проект активен
2. Проверьте, не на техническом обслуживании ли Supabase
3. Попробуйте использовать прямой URL, но добавьте параметры:
   ```
   postgresql://postgres:[PASSWORD]@db.[PROJECT].supabase.co:5432/postgres?connection_limit=1&pgbouncer=true
   ```

Но лучше связаться с поддержкой Supabase для получения правильного Connection Pooling URL.

