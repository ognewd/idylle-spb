# 📊 Статус деплоя

## ✅ Текущий статус

- **Сайт доступен**: https://idylle-spb.vercel.app
- **Статус**: Работает на старом деплое (26 дней назад)
- **Последние попытки деплоя**: Завершились с ошибкой

## ⚠️ Проблема

Последние деплои завершаются с ошибкой. Старый деплой работает нормально.

## 🔍 Что нужно проверить

### 1. Переменные окружения в Vercel

Перейдите на https://vercel.com/dognevs-projects/idylle-spb/settings/environment-variables

Убедитесь, что настроены:

#### Обязательные:
- `DATABASE_URL` - строка подключения к PostgreSQL (Supabase или другая облачная БД)
- `NEXTAUTH_SECRET` - секретный ключ (сгенерировать: `openssl rand -base64 32`)
- `NEXTAUTH_URL` - `https://idylle-spb.vercel.app`
- `NEXT_PUBLIC_BASE_URL` - `https://idylle-spb.vercel.app`

#### Рекомендуемые:
- `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS` - для отправки email
- Все переменные `COMPANY_*` - реквизиты компании
- `STRIPE_*`, `YOOKASSA_*`, `TINKOFF_*` - для платежных систем

### 2. Логи ошибок

Посмотрите логи последнего деплоя:
- https://vercel.com/dognevs-projects/idylle-spb

Откройте последний deployment с ошибкой и посмотрите Build Logs.

### 3. База данных

Убедитесь, что:
- База данных доступна из интернета
- `DATABASE_URL` правильно настроен
- Если используете Supabase, проверьте connection string

### 4. Проверка локальной сборки

Локально сборка проходит успешно:
```bash
npm run build
# ✅ Compiled successfully
```

## 🚀 Попытка повторного деплоя

После проверки переменных окружения:

```bash
cd /Users/dognev/idylle-spb
vercel --prod
```

## 📝 Что исправлено

1. ✅ Обновлен `next.config.js` - добавлены домены для изображений (vercel.app)
2. ✅ Исправлены ошибки TypeScript в `src/app/api/admin/import/parse/route.ts`
3. ✅ Локальная сборка проходит успешно

## 🔗 Полезные ссылки

- **Vercel Dashboard**: https://vercel.com/dognevs-projects/idylle-spb
- **Проект**: https://idylle-spb.vercel.app
- **Логи деплоя**: https://vercel.com/dognevs-projects/idylle-spb/deployments

